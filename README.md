# Setup

- [Install uv](https://docs.astral.sh/uv/getting-started/installation/) for package management (if you haven't already)
- run `uv sync` to create and sync a virtual environment based on the dependencies in `pyproject.toml`
- finally, launch the webpage by running in the terminal `uv run streamlit run Home.py`
